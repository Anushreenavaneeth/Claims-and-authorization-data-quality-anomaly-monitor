# Demo Flow Guide

Use the demo service to showcase platform capability:
1. Trigger normal claims ingestion (`POST /api/demo/start`).
2. Trigger an anomaly event (volumetric anomaly or schema drift).
3. Observe real-time WebSocket alerts on the frontend.
4. Assign investigation, consult RAG recommendations, and resolve the issue.
