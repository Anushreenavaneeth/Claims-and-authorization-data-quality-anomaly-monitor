# Deployment Guide

The platform is containerized using Docker and ready for orchestrators like AWS ECS or Kubernetes.

## Requirements
- Docker 20.10+
- Docker Compose 1.29+
- AWS IAM/Secrets Manager (optional, for AWS integrations)
