name: Deploy
on:
  push:
    branches: [main]
jobs:
  deploy:
    runs-on: ubuntu-latest
    steps:
      - name: Print Deployment Triggered
        run: echo "Code pushed to main, starting build & deployment sequence."
